TestCase('tiles tests', {
    'tileSlots lengthCheck':function () {
        assertFalse(false);
        //var tileboardView = new TileBoardView;
        //assertEquals('Incorrect tileSlots length', 4, tileboardView.tileSlots.length)
    }
});